# Screenshot Plan

Store final screenshot assets under `docs/assets/screenshots/`.

Recommended primary screenshots:

1. `chat-overlay-main.png`
   - Main centered chat overlay with transcript, composer, and footer thread state.
2. `workbench-compose-review.png`
   - Workbench tray + compose review with staged fragments and handle insertion.
3. `thread-session-controls.png`
   - Thread picker and thread settings surface showing model / effort / collaboration mode.
4. `request-inbox-approval.png`
   - Approval or request-user-input surface stacked above chat.

Recommended widths:
- full overlay captures: 1800-2200 px wide
- focused modal/picker captures: 1400-1800 px wide

Prefer PNG, 2x scale, and a clean terminal theme with readable contrast.

Current captured set:

- all four screenshots currently share the same size: `3510x2194`
- that ratio is already good for the README and release docs
- no resizing is needed unless you want smaller derived web assets later
